const { default: axios } = require("axios");
const getProfessorDetails = require("../controller/professor-details-controller");

const getCoreJsonPrompt = async (name, number) => {};

module.exports = getCoreJsonPrompt;
